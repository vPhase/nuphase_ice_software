[Unit]
Description=Copy files and delete old ones 

[Service] 
EnvironmentFile=/etc/nuphase.cfg 
Restart=always 
ExecStart=/bin/sh -c "${NUPHASE_PATH}/nuphase-copy" 
ExecReload=/bin/kill -USR1 $MAINPID

[Install] 
WantedBy=nuphase.target 
